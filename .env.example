# URL da API do backend (pasta server/). Em dev, o padrão é http://localhost:3001.
VITE_API_URL=http://localhost:3001

# Login com Google — Client ID do OAuth (Google Cloud Console).
# Sem este valor, o botão "Entrar com Google" não aparece (login por e-mail/senha continua).
VITE_GOOGLE_CLIENT_ID=
